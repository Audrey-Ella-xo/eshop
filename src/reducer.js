// Here, we'll define all the application level states and actions to make changes to the state

export const initialState = {
    basket: [],
}

// Selector

const reducer = (state, action) => {

}

export default reducer